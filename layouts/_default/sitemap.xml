{{- printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\"?>" | safeHTML }}
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"
  xmlns:xhtml="http://www.w3.org/1999/xhtml">
  {{- /* Respect per-page front matter overrides; exclude items with sitemap.priority = -1 */}}
  {{- $pages := where .Site.AllPages "Params.sitemap.priority" "!=" -1 }}
  {{- /* Drop taxonomy terms for tags while keeping their content pages */}}
  {{- $pages = where $pages "Data.Singular" "!=" "tag" }}
  {{- /* Drop taxonomy terms for categories to keep sitemap focused on main content */}}
  {{- $pages = where $pages "Data.Singular" "!=" "category" }}
  {{- range $pages }}
  <url>
    <loc>{{ .Permalink }}</loc>
    {{- with .Lastmod }}
    <lastmod>{{ .Format "2006-01-02T15:04:05-07:00" }}</lastmod>
    {{- end }}
    {{- with .Sitemap.ChangeFreq }}
    <changefreq>{{ . }}</changefreq>
    {{- end }}
    {{- if ge .Sitemap.Priority 0 }}
    <priority>{{ printf "%.1f" .Sitemap.Priority }}</priority>
    {{- end }}
  </url>
  {{- end }}
</urlset>
